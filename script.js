function searchGemini() {
  const query = document.getElementById('search-bar').value;
  if (query) {
    // Placeholder for Gemini AI search functionality
    alert(`Searching Gemini AI for images with query: ${query}`);
  } else {
    alert('Please enter a search query.');
  }
}
